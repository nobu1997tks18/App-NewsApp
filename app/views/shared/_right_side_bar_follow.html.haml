.user-data-links__title
  %h2 
    ="#{@user.name}さん"
  .links__options
    %ul.option-list
      %li
        =link_to "Follow #{user.followings.count}",followings_user_path(user)
      %li
        =link_to "Follower #{user.followers.count}",followers_user_path(user)
      %li
        =link_to "マイページへ", user_path(current_user)